# Makes the project root importable so `from caller import ...` works in
# tests without installing the package. Once this repo grows a pyproject.toml
# (Phase 3, for Docker packaging), this can move in there.
[pytest]
pythonpath = .
testpaths = tests
